# @package _global_

# hydra settings (logging disabled)
defaults:  
  - _self_  
  - override /hydra/hydra_logging@_group_: none
  - override /hydra/job_logging@_group_: none

hydra:
  output_subdir: null
  run:
    dir: .
  sweep:
    dir: .
    subdir: .